<%= render 'layouts/admin_header' %>

<div class="container">
	<div class="row">
		<div class="col-xs-8 col-xs-offset-2">
			<table class="table">
				<h2 class="title">顧客一覧</h2>
				<thead>
					<th>顧客ID</th>
					<th>顧客名</th>
					<th>会員ステータス</th>
					<th></th>
				</thead>
				<tbody>
					<% @customers.each do |customer| %>
						<tr>
							<td><%= customer.id %></td>
							<td>
								<%= customer.name_last_kanji %>
								<%= customer.name_first_kanji %>
							</td>
							<td>
								<% if customer.deleted_at.nil? %>
									<span style="color: lime;">有効</span>
								<% else %>
									<span style="color: red">退会済み</span>
								<% end %>
							</td>
							<td><%= link_to "詳細",admin_customer_path(customer.id), class: "btn btn-default" %> </td>
						</tr>
					<% end %>
				</tbody>
			</table>
			<div style="text-align: center;">
				<%= paginate @customers, class: "paginate" %><br>
			</div>

			<div style="text-align: center;">
				<%= link_to '管理者TOP', admins_path, class: "btn btn-default", style: "margin: 15px 0;" %>
			</div>

		</div>
	</div>
</div>

<%= render 'layouts/admin_footer' %>
